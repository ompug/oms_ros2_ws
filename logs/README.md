# Local evidence

Setup, build, and test commands write timestamped evidence here. Large and
machine-specific logs and package inventories are ignored by Git. The initial
apt source snapshot is retained so the pre-install repository configuration is
reviewable and recoverable.

